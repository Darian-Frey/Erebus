// Example: load a preset from disk, render to a 4K equirectangular PNG +
// EXR without opening a window. Mirrors the future CLI export mode.
//
// Phase 6 work — this is currently a placeholder so `cargo build --examples`
// stays green.

fn main() {
    eprintln!("erebus headless export — implemented in Phase 6 (see docs/ROADMAP.md)");
}
